pkgname=cockpit-openscap
pkgver=VERSION
pkgrel=1
pkgdesc='Cockpit OpenSCAP Module'
arch=('x86_64')
url='https://github.com/cockpit-project/openscap'
license=(LGPL)
source=("SOURCE")
sha256sums=('SKIP')

package() {
  depends=(cockpit)
  cd $pkgname
  make DESTDIR="$pkgdir" install PREFIX=/usr
}
